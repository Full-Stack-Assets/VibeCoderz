// Conductor uses hand-authored plain CSS (no Tailwind). This empty config
// overrides any PostCSS/Tailwind config inherited from a parent directory.
const config = { plugins: {} }
export default config
